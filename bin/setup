#!/usr/bin/env bash
# bin/setup — bootstraps Bundler then delegates everything to the PE CLI.
set -euo pipefail
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
gem install bundler --conservative
bundle exec predictability-engine setup
